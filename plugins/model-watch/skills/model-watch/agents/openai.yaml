interface:
  display_name: "模型哨兵"
  short_description: "在主任务前判断当前模型是否仍合适"
  brand_color: "#5168FF"
  default_prompt: "!model-watch on"
dependencies:
  tools:
    - type: "mcp"
      value: "model-watch"
      description: "保存设置、任务状态和模型判断结果，并提供设置界面。"
policy:
  allow_implicit_invocation: false
