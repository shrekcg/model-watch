{
  "name": "model-watch",
  "version": "1.1.1",
  "private": false,
  "description": "A pre-task model recommendation sentinel for Codex conversations and workflows.",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "update:plugin": "node scripts/update-plugin.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^3.25.76"
  }
}
