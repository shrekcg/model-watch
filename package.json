{
  "name": "model-watch",
  "version": "1.1.2",
  "private": false,
  "description": "A pre-task model recommendation sentinel for Codex conversations and workflows.",
  "type": "module",
  "scripts": {
    "test": "for file in tests/*.test.mjs; do node --test --test-concurrency=1 \"$file\" || exit 1; done",
    "update:plugin": "node scripts/update-plugin.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^3.25.76"
  }
}
